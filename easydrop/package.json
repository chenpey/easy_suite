{
  "name": "easydrop-worker",
  "version": "1.1.6",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "setup": "node scripts/manage.mjs local",
    "dev": "npm run build && WRANGLER_LOG_PATH=.wrangler/logs/ WRANGLER_SEND_METRICS=false wrangler d1 migrations apply DB --local && WRANGLER_LOG_PATH=.wrangler/logs/ WRANGLER_SEND_METRICS=false wrangler dev --ip 127.0.0.1",
    "preview": "npm run build && node scripts/preview.mjs",
    "deploy": "bash deploy.sh",
    "test": "npm run build && node --test test/api.test.mjs test/deploy.test.mjs ../test/reset.test.mjs",
    "test:ui": "npm run build && playwright test test/ui.spec.mjs --workers=1",
    "check": "node --check src/worker.js && node --check src/auth.js && node --check web/app.js && node --check scripts/manage.mjs && node --check scripts/cloudflare.mjs && node --check ../scripts/reset.mjs"
  },
  "dependencies": {
    "lucide": "1.45.0",
    "qrcode": "1.5.4"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "esbuild": "0.28.2",
    "miniflare": "5.20260911.0-alpha",
    "wrangler": "4.131.1"
  },
  "allowScripts": {
    "esbuild@0.28.1": true,
    "esbuild@0.28.2": true,
    "fsevents@2.3.3": true,
    "workerd@1.20260911.1": true
  }
}
